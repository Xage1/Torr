# Database# Database
DATABASE_URL=postgresql://postgres:password@localhost:5432/konnect_db?schema=public

# JWT
JWT_SECRET=replace_with_a_long_random_secret
JWT_EXPIRES_IN=7d

# App
PORT=4000
NODE_ENV=development
